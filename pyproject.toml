[project]
name = "browsertrix-harvester"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "bs4",
  "click",
  "jsonlines",
  "pandas",
  "requests",
  "sentry-sdk",
  "smart-open[s3]",
  "ultimate-sitemap-parser",
  "warcio",
]

[project.scripts]
harvester = "harvester.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["harvester*"]

[dependency-groups]
dev = [
  "coveralls",
  "coverage",
  "ipython",
  "mypy",
  "pandas-stubs",
  "pip-audit",
  "pre-commit",
  "pytest",
  "ruff",
  "types-beautifulsoup4",
]

[tool.mypy]
disallow_untyped_calls = true
disallow_untyped_defs = true
exclude = [
  "tests/",
  "output/",
]

[[tool.mypy.overrides]]
module = [
  "dateutil.*",
  "smart_open.*",
  "usp.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
log_level = "INFO"
markers = [
  "raise_smart_open_exception: include to have smart_open.open throw Exception",
  "unit: unit tests",
  "integration: integration tests",
]

[tool.ruff]
target-version = "py313"
line-length = 90
show-fixes = true

[tool.ruff.lint]
select = ["ALL", "PT"]
ignore = [
  "COM812",
  "D107",
  "N812",
  "PTH",
  "C90",
  "D100",
  "D101",
  "D102",
  "D103",
  "D104",
  "D415",
  "EM102",
  "G004",
  "PLC0207",
  "PLR0912",
  "PLR0913",
  "PLR0915",
  "PLR2004",
  "PT023",
  "S321",
  "TD002",
  "TD003",
  "TRY003",
]
fixable = ["E", "F", "I", "Q"]

[tool.ruff.lint.flake8-annotations]
mypy-init-return = true

[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false

[tool.ruff.lint.per-file-ignores]
"tests/**/*" = [
  "ANN",
  "ARG001",
  "S101",
]

[tool.ruff.lint.pycodestyle]
max-doc-length = 90

[tool.ruff.lint.pydocstyle]
convention = "google"